import { Text, View } from "react-native"

const Popular = ()=>{
    return (
        <View style={{flex: 1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color: '#0000FF', fontSize: 40}}>Phổ biến</Text>
        </View>
    );
};
export default Popular;