import { Text, View } from "react-native"

const Favourite = ()=>{
    return (
        <View style={{flex: 1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color: '#0000FF', fontSize: 40}}>Yêu thích</Text>
        </View>
    );
};
export default Favourite;