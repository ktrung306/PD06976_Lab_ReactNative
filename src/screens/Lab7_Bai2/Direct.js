import { Text, View } from "react-native"

const Direct = ()=>{
    return (
        <View style={{flex: 1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color: '#0000FF', fontSize: 40}}>Trực tiếp</Text>
        </View>
    );
};
export default Direct;