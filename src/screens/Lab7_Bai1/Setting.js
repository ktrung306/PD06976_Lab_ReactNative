import { Text, View } from "react-native"

const Setting = ()=>{
    return (
        <View style={{flex: 1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color: '#0000FF', fontSize: 40}}>Setting Screen</Text>
        </View>
    );
};
export default Setting;