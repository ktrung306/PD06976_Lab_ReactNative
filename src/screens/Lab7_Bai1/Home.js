import { Text, View } from "react-native"

const Home = ()=>{
    return (
        <View style={{flex: 1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color: '#0000FF', fontSize: 40}}>Home Screen</Text>
        </View>
    );
};
export default Home;