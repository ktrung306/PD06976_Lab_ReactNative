import { Text, View } from "react-native"

const Phone = ()=>{
    return (
        <View style={{flex: 1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color: '#0000FF', fontSize: 40}}>Phone Screen</Text>
        </View>
    );
};
export default Phone;