import { Text, View } from "react-native"

const Content = ()=>{
    return (
        <View style={{flex: 1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color: '#0000FF', fontSize: 40}}>Content Screen</Text>
        </View>
    );
};
export default Content;